# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                @controllers.LogInController.index

GET         /usuarios        @controllers.LogInController.traerTodosLosUsuarios
POST        /usuario         @controllers.LogInController.agregarUsuario
